# AGENTS.md — 給 AI 助手的專案說明

這份檔案給任何在這個 repo 工作的 AI coding agent（Codex / Claude Code / 其他）看。
先讀完再動手，可以省掉重新摸索的時間，也避免踩到已經解過的坑。

---

## 這個專案在做什麼

**一句中文需求 → 可施工的 DXF 建築平面圖。**

```
「透天三層，基地 19×13 米，三房，地下一層車庫」
        ↓
  平面圖 / 剖面 / 立面 + 圖框標題欄 + 門窗家具 → DXF / PDF
```

Python + [ezdxf](https://ezdxf.mozman.at/)，是一個學生專題。使用者不是資深軟體工程師，
**解釋技術問題時請用生活化比喻，不要堆術語。** 專案的長期目標是「自動設計出對得起
真實審定圖的平面圖」，不只是幾何正確。

---

## 環境與常用指令

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt

python -m pytest -q                    # 全套測試，目前 ~986 passed，必須全綠
python -m src.design.benchmark         # 34 案巡檢 → output/benchmark/report.html
uvicorn src.web.app:app --reload       # 網頁版 http://localhost:8000
```

- 網頁版與 AI 設計師模式需要環境變數 `GEMINI_API_KEY`。
- ⚠️ Gemini 免費額度 **每日 20 次**，AI 模式一次請求吃 2~3 次呼叫（約 7 次/日就用完）。
  除錯時盡量用測試裡的假 client，不要真的一直打 API。
- 產出都在 `output/`（已 gitignore），整個刪掉不影響程式。

---

## 兩條產線（很重要，別搞混）

| | 規則版 | AI 設計師版 |
|---|---|---|
| 入口 | `src/design/layout_generator.py` → `generate_floor_plan` | `src/design/layout/design_loop.py` → `design_building` |
| 怎麼決定格局 | 寫死的建築規則 | Gemini 產「房間關係圖」→ BSP 落實 |
| 適用 | 一般基地、集合住宅 | 只做窄透天：建築寬 5~30m、深 ≥9m |
| 網頁怎麼選 | `src/web/app.py` 的 `_generate_auto` 自動選，AI 掛掉自動退回規則版 | 同左 |

**改東西時先確認你在改哪一條。** 兩條產線共用的只有：`plan_check`（圖面關卡）、
`narrow_house._fix_openings`（收門洞）、`_add_stair_guard_walls`（樓梯導牆）、
Phase 6 家具擺放。

### AI 設計師版的流程

```
自然語言 brief
  → room_graph.propose_room_graph   （Gemini 當設計師，只出房間關係，不出尺寸）
  → sanity_check                    （關係圖本身合不合理）
  → graph_layout.realize_graph_building （關係圖 → 實際切割 + 柱 + 家具 + 動線修復）
  → plan_check                      （硬規則關卡，不合格就換切法重生）
  → design_loop.critique_building   （挑毛病）
  → room_graph.refine_room_graph    （Gemini 重新設計）→ 迴圈
```

收斂迴圈的 fitness = `平均分 − 2 × 問題數`，**只留最高分版本，絕不退步**。

---

## `plan_check.py` — 什麼叫「合格的圖」

`src/design/layout/plan_check.py` 把圖面正確性寫成硬規則。這是整個專案的品質底線，
**動格局相關的程式碼前一定要先讀它**。

**error（擋圖重生，換個切法就能解）：**

1. `room_no_door` 房間沒門
2. `floor_split` 同層室內斷成兩塊
3. `no_entry` 1F 沒大門
4. `entry_upstairs` 樓上外牆開門（門通往空中）
5. `furniture_in_wall` 家具穿牆
6. `door_in_corner` 門卡在房間角落
7. `stair_blocks_door` 門直接開在階梯上（缺起步平台）
8. `stair_side_open` 梯段有一側沒牆（會從旁邊掉下去）
9. `circulation_blocked` 動線走不通
10. `opening_on_column` 門窗開口壓在柱上（柱穿過窗框／門洞）

**warning（只回報，要改設計才救得動）：** `room_no_daylight`、`room_oversize`、`room_skinny`

分界原則：**error = 同一份房間關係圖、換個切法就能解決**；warning = 非改設計不可。
新增規則時請照這個原則歸類，錯類會讓產線陷入無限重生。

---

## 已經踩過的坑（別再犯）

- **共壁 vs 獨棟**：透天共壁不能四面開窗，但**中庭骨架是獨棟、四面都能開**。
  誤把共壁規則套到獨棟會讓採光分數掉到 0~50%。用 `party_walls` 開關控制。
- **房間不能用名稱比對**：同一層可能有兩間「臥室」，用名稱找會永遠修錯間。用 id。
- **房間多邊形走的是牆中心線**，擺家具要縮到牆內面（`_inner_room`），否則家具穿牆。
- **家具碰撞尺寸 ≠ 繪圖尺寸**：擺位器用的尺寸較小、畫圖較大，沙發會畫到牆外。
- **`ai_extra` 永遠非空**（裡面帶 engine），要判斷是不是 AI 出的圖請用 `.get("ai_design")`。
- **門角淨距**分級退讓 350/250/150/100，全產線共用，不要各自寫一套。

---

## 設計原則（使用者定調過的，別自作主張改）

- **走道** = 多房間共用的動線才設。房間少的小宅，動線融入客廳，不要硬塞走廊。
- **柱網**：規則等距、跨度 6~9m 最經濟、柱要藏在牆內不卡開口、多層一定上下對齊。
  柱**斷面**由 `design/column_design.py` 逐層概算，別在任何地方寫死尺寸。
- **Report 慣例**：每個新模組的 Report 類別都要有 `.summary()`（給人看）
  和 `.to_dict()` / `.to_json()`（給程式吃）。

### 柱斷面：`design/column_design.py`（2026-08-07）

原本全棟柱一律寫死 500×500。現在照真實設計師的做法，逐層由**負擔面積法**概算：

```
負擔面積（周圍到隔壁柱各半跨）× 上方樓層數 × 單位重 → 軸力
軸力 ÷ 容許軸壓應力(0.35f'c) → 斷面積 → 邊長（5cm 級距、守 30cm 下限）
```

⚠️ **這是概算，不是結構計算**（不算地震力、彎矩、配筋，那是技師簽證的責任）。
模組檔頭有完整免責說明，動它之前先讀。

**跑出來的事實**：住宅的柱幾乎都被規範最小尺寸壓在 30cm——低層住宅的柱根本不是
被「壓垮」決定的，是被**耐震**決定的。所以另補一張 `EMPIRICAL_START`
**實務起始斷面對照表**（⚠️ 經驗值，沒有法源），最終採用 = `max(重力概算, 經驗值)`。
Report 的 `governed_by` 會講明每層是「重力」還是「經驗值」控制，不能讓人誤以為全是算的。

**兩個關鍵設計決定：**

1. **只縮不放。** 概算結果永遠 ≤ 原本的 500，而縮小的柱一定完全落在原大柱範圍內
   → 不可能新撞到門窗家具，所以不必重排格局，放在格局定案後套用就好。
   放大就得重排（`_blocked` 開口躲柱的區間會變大），那是另一回事。
2. **`check_column_alignment` 從「柱心距離 ≤50mm」改成「上層柱斷面完全落在下層柱內」。**
   真實建築的柱往上縮常常是**對齊某一面**、不是同心縮，柱心因此差幾公分但力路徑
   完全連續。用柱心距離判會把這種正確做法誤判成錯誤。兩層同尺寸時退化成原判準。

**柱位微調要跟著縮**（`_shrink_tuck`）：兩帶式為了不讓柱探進客廳，把分界牆那排柱
往北推了 `柱寬/2 − 牆厚/2`。柱一變細，推移量沒跟著改的話柱會離開牆面、在牆線上開缺口。

**成效（19×13 三層實測）**：柱探進房間的面積 7.47m² → 2.20m²，**少 71%**。

**柱座落品質量測** `column_seating(spec)`：交點柱 / 邊柱 / 孤柱。改柱網演算法時拿它
驗證，別憑感覺。現況地上層**孤柱 0、藏牆率 100%**（已有測試釘住）。
⚠️ 地下停車場藏牆率只有 72~79% 是**正常的**——那層本來就沒隔間牆，別當 bug 追。

### 設計原則：盡量讓室內的人看不到柱子（使用者 2026-08-07 定調）

比「柱藏在牆內」更嚴格——柱坐在牆上，柱角照樣會凸進房間（柱比牆胖）。
量測用 `column_visibility(spec)`：柱斷面扣掉牆的實體，剩下的再與**縮到牆內面的
房間**相交，交到的才算「室內看得到」。凸到室外、凸進牆體、凸進管道間都不算。

兩個手段，**效果差很多**：

1. **把柱變細**（上面的斷面概算）——這是主力。實測讓室內看得到的柱角少 74~86%。
2. **外牆柱往室外推**（`_push_exterior_out`）——外牆柱有內部柱沒有的特權：可以往
   室外躲，推到室內那一面與牆內面齊平，室內牆面完全平整；凸出的部分變成立面的
   垂直線條（真實建築就是這樣處理的）。加分項，不是主力。

⚠️ **踩過的坑**：判斷「柱在不在建築外緣」要用牆的**中心線**算外框，不能用牆實體
（buffer）的外框——柱心坐在中心線上，離牆外皮有半個牆厚（100mm），用外皮判會一根
外牆柱都認不出來，整個功能靜悄悄地不生效。

### `STRUCT_MARGIN`：替外牆柱留位置（2026-08-10，這才是真正的解）

上面第 2 個手段寫完之後**一根都推不動**，當時以為「柱不得越過建築線，推不動是對的」。
真正的根因不在柱，在**我們根本沒替柱留位置**：

```
外牆中心線 = 基地 − 退縮 = 建築線     ← 柱想外推就越過建築線 → 拒絕推
順帶：牆厚 150，外皮早就越過建築線 75mm（實質違建）
```

`layout_generator.STRUCT_MARGIN = 275` 把外牆**中心線**從建築線再往內縮：

```
275 = 柱寬 350 − 牆厚/2 75
    ＝ 柱推到「室內面與牆內面齊平」時，柱外緣到中心線的距離
```

⚠️ **鐵則：留柱位不得讓原本生得出來的案子生不出來**（那是拿「有圖可用」換「圖好看」）。
所以 `_house_frame` / `_generate_house` 兩個入口都做成**退讓階梯**——275 → 250 → …
→ 0，先試留滿，放不下就少留一點。不能只拿 W/D 下限去夾：真正放不下的原因常在更後面
（臥室最小寬、樓梯間進深、餐廳併不併），要排完才知道。整條階梯跑到底只花 0.07ms。
級距 `STRUCT_MARGIN_STEP = 25`——粗階梯會讓「其實留得起 220」的基地一路掉到 0。

**成效**：19×13 三層 **外牆柱露臉 12 根 → 0 根**、0.854 → 0.191 m²（−78%）；
12×11 這種卡在下限的照生（退到 0）。掃描：兩帶式 47 → **49** 合格（不減反增）。

### 柱是**實心的**：家具不能壓在上面（使用者 2026-08-10 指出「柱卡到沙發」）

`column_footprints(spec)` 是柱實體的單一出處。以前專案只有「牆的實體」，擺位器與
圖面關卡**都當柱不存在** → 實測 18×13 三層有 10 件家具壓在柱上。

**優先順序：穿牆 > 擋門 > 壓柱。** 三件事嚴重程度不同：

| | 處理 |
|---|---|
| 沙發/衣櫃/鞋櫃 | 先沿牆讓到旁邊，不行才往室內挪（`clear_fixtures_off_columns`）|
| 流理台 | **截短**（`trim_counters_at_columns`）——師傅遇到柱是切短，不是整排離牆 |
| 閃不掉 | **不動**，`plan_check` 記一筆 **warning**（不是 error）|

⚠️ **`furniture_in_column` 必須是 warning**：修復是盡力而為，列成 error 會讓產線為了
修不掉的東西無限重生、網站對其他都合格的圖回 422。

⚠️ **踩過兩次的坑**（都是「好心把柱當成跟牆一樣重要」）：
1. 把柱併進 `fixture_fix._wall_union`（= 壓柱與穿牆同級）→ 鞋櫃為了閃柱退進門的開啟
   弧，18.4×14.5m/seed4242 從「生得出來」變「生不出來」。
2. `settle_fixtures_to_wall` 第一版是「所有家具都盡量往牆邊推」→ 把
   `_declutter_for_circulation` 特地挪開讓出通道的家具又推回去，淺透天掃描冒出 4 案
   `circulation_blocked`。**挪動家具的模組不只一個，誰都不該去動別人的決定**——現在
   只還原自己挪過的、而且只在它之後沒被別人動過（靠 `DODGE_MARK` 記位置比對）。

⚠️ **順序**：擺家具時柱還是舊的 500、還沒外推，貼牆家具會為了閃柱讓開；
`apply_column_design` 把柱縮細又推到室外後**一定要叫它回來**，否則沙發停在離牆 18cm
的半空中。還原的呼叫點在 `apply_column_design` 尾端，不在 `push_fixtures_out_of_walls`。

**合計成效（三層透天實測）**：室內看得到的柱角 **3.3~4.2 m² → 0.19~0.29 m²**；
壓在柱上的家具 **10 件 → 0 件**（家具一件都沒少，是挪開不是刪掉）。

### 門也不能撞到柱：`column_centers is None` 的盲點（2026-08-11）

`door_rules._swing_obstacles` 以前寫 `spec.column_centers or []`。但

```
column_centers = None   ← 「柱放在每個軸網交點」（窄透天 / 淺透天 / AI 產線都這樣存）
None or []  ==  []      ← 一根柱都看不到
```

所以「門扇打開會不會掃到柱」這條規則，**對那三條產線從來沒有生效過**。
把柱位具體化（外推柱時本來就必須這麼做）之後才第一次現形。

**取柱一律走 `column_design.column_footprints(spec)`**（柱實體的單一出處，會把
`None` 解成實際柱位），不要自己讀 `column_centers`。

⚠️ **時序**：柱是在格局定案**之後**才排的，所以 `_fix_openings` 與第一次
`repair_doors` 看到的是「還沒有柱」的世界。窄透天／淺透天都在 `_set_structural_grid`
之後**再修一次門**；AI 產線本來就在柱之後修。修法沿用既有機制：先轉門把
（換鉸鏈邊／開啟方向共四種），都不行才改橫拉門。

### 門窗開口不能壓在柱上（2026-08-20，使用者指著 CAD 說「柱子會在窗戶裡面」）

跟 `column_centers is None`、`單跑直梯` 是**同一種**盲點：**規則存在，但關卡沒接**。

```
layout_generator.validate_spec  有「洞口壓柱」這條 → 但它只有 benchmark 和測試在呼叫，
                                   產線（generate_building_auto / 網站）從來不跑它
plan_check                      是產線真正的關卡 → 但它沒有這條
```

所以四條產線裡只要有一條的躲柱機制漏掉，圖就這樣出去了，沒有人擋。
現在 `plan_check` 第 10 條 `opening_on_column` 補上，判準是 `column_bite()`
**柱吃掉洞口沿牆幾 mm**（不是面積 —— 使用者看圖問的是「這扇窗被吃掉多寬」）。

⚠️ 判準只認**真的重疊**，不是 `COLUMN_CLEARANCE`（300mm 淨距）。那 300 是產生端
排洞口時要留的餘裕，拿來當關卡會把只差幾十 mm、其實蓋得出來的圖也擋掉。

**是 error 不是 warning**（跟 `furniture_in_column` 剛好相反）：柱穿過窗框根本蓋不
出來，而且開口沿著牆挪就能解 —— 四條產線都有躲柱機制（`_column_blocks` /
`_blocked`），完全符合「換個切法就能解」的分界。

實測災情：規則版三條產線（窄透天 50 案、淺基地 50 案、兩帶式 40 案）**一件都沒有**，
它們排洞口時就在躲柱（兩帶式甚至寧可 raise 也不把窗放在柱上）。
出事的是 **AI 產線**，而且**門窗都會**：

- **窗**：2026-08-19 已補 `shift_windows_off_columns`（柱定案後再修一次）。
- **門**：以為 `repair_doors` 會兜底 —— **錯了**。那支修的是「門扇打開會撞到什麼」，
  門洞**本身**跨在柱上它不管：柱在牆內、只凸出牆面 (柱寬−牆厚)/2，門開 90 度掃過的
  方塊常常閃得過去，於是 `door_swing_blocked` 一聲不吭，圖上卻是柱穿過門框。

所以那支改名 **`shift_openings_off_columns`**，門窗一起收。門比窗多一道限制：
挪完仍要 `_door_pos_ok`（離房間角落夠遠），否則躲柱只是換成 `door_in_corner`、
問題從左手換到右手；門角淨距走全產線共用的 `DOOR_CLEAR_STEPS` 退讓階梯。

### 柱網的驗收標準：柱藏牆內 **且** 跨度等分（使用者 2026-08-21 定調）

使用者指著一張 CAD 圖說「這張圖的柱子設計，我要之後都照這個設計」，追問後確認
是**兩件事都要**，不是二選一：

```
① 柱藏在牆內   —— 量 column_seating：孤柱 0、藏牆率 100%（最好全是交點柱）
② 跨度規則等分 —— 量 grid_regularity：最大/最小 ≤1.15 倍、全落在 6~9m
```

⚠️ **這兩件事會互相拉扯，而且很容易只做到一半：**

| 誰跟著誰 | 結果 |
|---|---|
| 柱線跟著牆走（舊做法） | 柱一定在牆裡 ✅，但跨度是副產品 → 30m 寬跑出 `[13.3, 3.4, 13.3]` ❌ |
| 牆跟著柱線走（現在） | 跨度等分 ✅，但柱線處**必須**生一道隔間牆，否則柱浮在房間中央 ❌ |

所以 `_realize_floor_core` 的柱線牆是**必做**、不是「房間夠多才做」的加分項
（房間不夠就補溢位房）。實測降級成加分項：藏牆率 100% → 91.7%、每層冒出孤柱。

**兩個量測都在 `column_design.py`，改柱網演算法時兩個都要看，只看一個會自我感覺良好**：
`column_seating` 一直回報 100%（柱確實都坐在牆上），但同一時期的柱網其實是
`[13.3, 3.4, 13.3]` —— 那正是使用者說「柱子的位子不合邏輯」的東西。

⚠️ **只修一個方向 = 只做一半。** 面寬改成 `bay_lines` 等分之後，進深仍留著舊公式
`round(D / 6000)`，肉眼看不出來，是 `grid_regularity` 把它指出來的（30 案掃描有 8 案
不合格，全是這個原因）。兩個方向都要走 `bay_lines`。

⚠️ **跨數越少越好**，只要不超過 9m 上限：9.0m 深的房子就是一跨，不要切成兩跨 4.5m
（實測同一棟 14.94×9.0m：9 根柱 → 6 根，少一排柱、少一排梁，而且每根都是交點柱）。

**現況**：AI 產線（`graph_layout`）已達標。**規則版兩帶式只做到一半**，原因在下面。

#### ⚠️ 兩帶式的 X 方向做不到等分（2026-08-21 實測後的結論，不是還沒做）

Y 方向做完了：進深在 9m 以內就**整排拿掉分界牆那排柱**（分界牆照樣是牆，只是牆裡
不站柱）。柱少 1/3，Y 跨度全部落進 6~9m。拿掉柱是安全的——少一根柱不會有人凸進房間。

X 方向試過等分，**42 條測試倒**，失敗訊息都是同一句：`軸線 x=… 上沒有豎牆（柱會凸
在房間裡）`。根因是幾何上兜不攏：

```
建築 14.45m 寬、北帶 3 間臥室 → 隔牆每 4.8m 一道
經濟柱距 6~9m                 → 柱線每 7.2m 一條      4.8 與 7.2 永遠對不上
```

只能二選一：柱線跟著牆（跨度不規則）或柱線等分（柱角凸進房間）。要兩者兼得就得把
臥室隔牆搬到柱線上——那會生出「一間 7.2m、兩間 3.6m」的臥室，是拿使用者體驗換一張
漂亮的柱網圖，已判定不做。

⚠️ **踩過的量測坑**：改完 X 之後 `column_seating` 回報「孤柱 0、藏牆率 100%」，
看起來完全成功。但那支問的是「有沒有**任何**牆的中心線穿過柱」——柱坐在外牆或分界
牆上就算通過。**它不回答「柱角會不會凸進房間」**，那要用 `column_visibility`。
用錯尺就會宣告一個其實失敗的改動成功。

**架構層面的結論**（值得寫進報告）：AI 產線做得到、兩帶式做不到，差別在生成順序——
AI 產線**先畫柱線再切格子**，牆一定長在柱線上；兩帶式**先定幾房幾寬再放柱**，柱只能
遷就牆。結構要求要放在生成順序的前面，才可能同時滿足。

### 往真實街屋的參考平面靠:開放餐廚 + 天井 + 1F 車庫(2026-08-26)

使用者給了兩張連棟街屋的 1F 參考平面(4.5×15m 基地,有車庫版/無車庫版),說
「格局幫我往這個方向設計」。對照下來,前後串聯的順序本來就一樣,差三件事:
**開放餐廚**、**天井**、**1F 車庫**(三件都做了)。

#### 開放餐廚:1F 後段併成一間

參考圖上寫的是「餐廳/廚房」。4.5m 面寬硬切成兩間,廚房只剩 2m 寬(走廊狀),
中間那道牆在生活上也沒有意義。改成一間 `("dining", "餐廚")`,面積上限是
**兩間相加**(`plan_check.MERGED_ROOM_PARTS` 早就這樣算);大到連併合上限都撐
不住的後段(8×18m 的 44㎡)才切兩間。

⚠️ **又一次「同一件事、兩個地方兩把尺」**:`plan_check` 知道「餐廚 = 餐廳+廚房」,
但**擺家具的那支不知道** —— 它只看 `kind`,於是開放餐廚只擺得出餐桌、沒有流理台。
`auto_furnish` 現在也走 `MERGED_ROOM_PARTS`(`_merged_kinds` / `_program_for`),
併合房拿兩份家具清單接起來。這個 bug 類型在本檔已經是第四次。

#### ⚠️ 天井**不會**讓你蓋得更深(實測 0.0m,別再試一次)

天井補回來了(位置就是參考圖上的位置:中段核裡、貼著浴廁,吃掉服務格裡浴廁
沒用完的那一塊),`code_check` 也補了 §41(開向天井的窗算採光,以前 `_is_exterior`
只認建築外緣 → 天井開了窗也不算 → 天井完全沒有意義)。

**但它買不到進深。** 六個面寬直接量建築進深,有無天井**一模一樣**
(3.5m 都是 12.5m、5.45m 都是 15.5m、7.45m 都是 16.5m;浴廁在南在北都試過):

> 天井只貼著服務格的一側,只服務得到前段**或**後段其中一段;而進深是前後
> 對稱長的 —— 擋住你的永遠是「沒貼到天井的那一段」。要靠天井加深,得做成
> 貫穿整個面寬的**天井帶**(就是 2026-07-29 拿掉的那個),那會吃掉一整排樓地板。

⚠️⚠️ **第一次量出來是「一律 +2.0m」,那是假的** —— 探測跑的是
`generate_narrow_building`,它裡面有進深退讓階梯(最多退 4×500 = **2000mm**)。
量到的「輸入 16m 還是乾淨的」其實是退讓救回來的,不是天井的功勞;「每個面寬都
剛好 +2.0m、剛好等於退讓上限」就是線索。**量骨架能力要繞過退讓階梯直接叫
`_build_floor`,而且量「實際蓋出來的深度」,不是「輸入多少還不會爆」。**
這是本檔「報表會說謊」系列的又一則。

所以天井是**設計選擇**不是採光手段:每層固定花掉約 3㎡(貫穿到屋頂,每層都少),
換到浴廁/樓梯間有自然採光通風。**預設關**(與 2026-07-29 拿掉天井後的行為一致),
要開走 `HouseBrief.patio` / `generate_narrow_building(patio=True)` /
`preview_plan.py --patio`。

#### 1F 車庫:**不插第四帶**,是把前段整段拿去停車

第一直覺是「車庫 | 客廳 | 核 | 餐廚」四個帶。**不能這樣做** —— 多插一帶會把 1F
的中段核往北推,樓上的核卻沒跟著推,**樓梯上下就對不齊**了(這條產線的根本要求)。
正解是**只改前段的下限**:有車庫時前段最小進深從 3300 換成一個法定車位長
(5500,`_zones(garage=True)`),1F 的前段整段是車庫。每層的核位置完全不變。

⚠️ **代價是 1F 沒有客廳,客廳往上挪到 2F 前段** —— 這正是台灣透天最常見的做法
(二樓客廳接前陽台,實測出來的圖就是這樣)。**不把客廳塞在車庫北邊是故意的**:
那間房南邊車庫、北邊中段核,一面外牆都沒有,§40 採光直接違規。連帶:
**一層樓的透天不能配車庫**(會連客廳都沒有),`generate_narrow_building` 直接擋。

⚠️ **車庫要多一整個車位的進深**,而窄面寬的進深上限是被 §40 採光卡死的:

| 建築面寬 | 配車庫需要的進深 | 該面寬的採光上限 | |
|---|---|---|---|
| 3.5m | 13.1m | 12.5m | ❌ 放不下,`_check_dims` 明白擋掉 |
| 4.0m | 13.1m | 13.45m | ✅ |
| 4.5m | 13.1m | 14.45m | ✅(參考平面那張) |
| 7.45m | 13.1m | 16.45m | ✅ |

用**基地**尺寸講的話還要再套建蔽率:住宅區 60% 要基地深 **21.8m** 才配得起車庫,
商業區 80% 也要 **16.4m**。「基地 4.5×15m + 車庫」在哪一區都生不出來 —— 那正是
使用者那張參考圖建蔽率高達 93% 的原因,舊市區既存街屋才做得到。

#### ⚠️ 車不是「擺家具」,是**停進車位**;而且是**門閃車,不是車閃門**

車庫空著不畫車,圖上就是一間空房。但把 `"car"` 加進 `FURNITURE_PROGRAM` 之後
**一台都擺不出來**,兩個原因疊在一起:

1. `car` 是 `CENTER_ORIGIN` 家具,擺位器只在房間**正中央**試 9 個點(中心 ±step)。
   車位的位置是被車庫形狀決定的,不是「挑個好位置」——擺位器的前提不成立。
   → `auto_furnish._park_car` 自己列候選:先靠捲門那一端,再置中/靠西/靠東。
2. 4.5m 街屋的車庫內深 5.53m、車長 4.6m,只剩 **925mm** 餘裕;而一扇內門的
   開啟弧就要 **850mm** → 實測 6 個候選**全部**被 `door_swing` 打回。

第 2 點的解不是把車擠小,是**讓門去閃車**:`_build_floor` 在擺完家具之後本來就
還會再跑一次 `repair_doors`(轉門把 → 改橫拉門)。真實車庫的門也不會往車頭上開。
所以 `_park_car` 建碰撞引擎後把 `engine.doors` 清空,順序才對。

#### 捲門:借橫拉門的畫法,但字要寫對

捲門往上捲,平面上**沒有開門弧**,畫法與橫拉門相同 —— 但圖上不能寫「拉門」,
師傅要知道那是鐵捲門。`Door` 因此多一個 `label`(預設「拉門」),
`_mirror_spec` / `unit.py` 的鏡射都要帶著它走(漏掉就會鏡射完變回「拉門」)。

⚠️ **人行大門塞不塞得下是量出來的**:牆角淨距 350×2 + 捲門 2500 + 牆垛 600 +
大門 1000 = **4800mm**。4.5m 面寬放不下,捲門本身就是大門(`no_entry` 認的是
「外牆上有門」)—— 真實做法是在捲門上開一扇小門,平面上仍是同一個洞口。
5.45m 以上才會多畫一扇人行門。

### 2F 主臥 + 更衣室:照使用者的 2F 參考平面(2026-08-27)

使用者給了 2F 的參考平面(4.5×15m:前陽台|主臥室+更衣|樓梯+天井+衛浴|次臥|後陽台)。
骨架本來就長這樣,差的只有兩件:**主次之分**與**更衣室**。

* **主臥在「最低的那個前段是臥室的樓層」** —— 一般是 2F;1F 讓給車庫時 2F 前段是
  客廳,主臥就上 3F(`_master_level`)。前段大到要切兩間時,西半間仍然是主臥。
* **更衣室切在貼著浴廁那一段北牆** —— 那段本來就開不了主臥的門(門要開向樓梯間),
  拿來當更衣室剛好。kind 用 `storage`:不必採光、門 75cm 級。

⚠️ **三個踩到的坑,每一個都是既有規則沒涵蓋新東西:**

1. **`canonical_room("master_bedroom")` 沒對映回 `bedroom`** → `FURNITURE_PROGRAM`
   查不到 → **主臥一件家具都擺不出來**(擺位器不報錯,只是什麼都不擺)。
   `ROOM_ALIAS` 補一條。
2. **`through_bedroom` 把更衣室當缺陷**:規則本來只豁免「套內衛浴」,更衣室是同一
   件事(只跟一間臥室相連的附屬空間)。抽成 `ENSUITE_KINDS = BATH_KINDS |
   {storage, utility}`。⚠️ 豁免的關鍵條件是**只有一個鄰室**:兩個以上鄰室的儲藏
   就是公共儲藏,藏在臥室後面才真的是動線缺陷。
3. **更衣室害得主臥擺不下床**:主臥多一個要走得到的目的地,
   `_declutter_for_circulation` 為了保住通道**把床搬走**。一間沒有床的主臥比沒有
   更衣室糟得多 → `_beds_ok` 量過,擺不下就退掉更衣室重生
   (與 `_fit_service` / `_fit_margin` 同一條鐵則:加分項不得讓本來好好的東西壞掉)。
   實測 4.5m 面寬留不住更衣室、6m 以上留得住。

⚠️ **順帶修掉一個「核不同構」的 bug(我自己改出來的)**:主臥改用自己的面積尺
(36㎡ vs 次臥 27㎡)之後,主臥層就不必切房了 → 那層不觸發 `min_service` →
**2F 的浴廁比 1F/3F 寬 1㎡、牆上下對不上**。「服務格要不要壓到最窄」是**整棟**的
決定,不是各層各自決定的 —— 判準改成拿「最需要壓的那種樓層」(臥室,上限最小)
當探針。`test_core_is_identical_on_every_floor` 釘住這條。

### 家具要配得完整:空房間是不完整的圖(2026-08-27)

使用者說「家具也都幫我設計進去」。先**量現況**(每種房間實際擺到什麼)才發現
三個洞,每一個都不是「擺位器不夠強」,而是**規則沒涵蓋到**:

| 洞 | 根因 | 解法 |
|---|---|---|
| 更衣室**全空** | 衣櫃 1500 寬 > 更衣室淨寬 1350;且門一開就掃掉大半個房間(12 個候選全被 door_swing 打回) | 新家具 `closet_rail` 1000×600 + `DOOR_DODGES_KINDS` 讓**門去閃設備** |
| 18 間浴室有 **14 間沒得洗澡** | 浴缸 1600×750 塞得進 3.3㎡ 的浴廁,但塞進去動線就不通 → `_declutter` 整個移掉 | 新家具 `shower` 900×900;小於 `BATHTUB_MIN_ROOM_M2`(4.5㎡)**直接畫淋浴間不試浴缸** |
| 有臥室**沒有床** | `_declutter_for_circulation` 只看動線,不知道床是臥室的重點(實測移掉 6 張) | `restore_essentials` 換**小一號**的補回來(雙人→單人、浴缸→淋浴間) |

⚠️ **「擺不下就略過」是靜默的**:擺位器擺不下不會報錯,只是什麼都不擺,所以
這三個洞在關卡上完全看不出來 —— **只有逐間清點家具才找得到**。改家具相關的
東西之後要跑一次清點,不要只看 plan_check 全過。

#### ⚠️ 順序決定結果,而順序要量出來

* **洗澡設備先擺**:它是浴室裡最大又最不能將就的東西。先擺馬桶洗手台 → 只補得到
  6/18 間;先擺它 → 10/18,而且馬桶洗手台**一間都沒少**。
* **床頭櫃跟著床擺**,不交給擺位器:擺位器只知道「靠牆、分數高」,兩個床頭櫃會
  並排擠在同一面牆上。位置是被**床**決定的(床頭兩側各一)→ `_add_nightstands`。
* **沒有床就不能留床頭櫃**(`drop_orphan_nightstands`):床可能後來被動線修復器
  移掉,留下兩個貼牆的小方塊,圖上看起來像漏畫。

⚠️ **整間忽略門弧會反效果**:浴廁一開始就忽略門弧的話,淋浴間會挑到擋門的位置,
反而有 4 間連馬桶都擺不下(18→14)。正解是**兩輪**:先讓設備閃門,真的擺不下
才讓門去閃設備(`_place_bathing`)—— 擺完 `repair_doors` 會把門收成拉門,
真實圖上的小浴室與更衣室本來就是拉門。

⚠️ **收尾要四條產線都接**(`settle_after_declutter`):`restore_essentials` +
`drop_orphan_nightstands` 包成一支,每個 `_declutter_for_circulation` 後面加一行。
本檔「規則存在,但關卡沒接」已經是第五次了。

#### 1F:三個洞都不是「擺位器不夠強」(2026-08-27,使用者:「1樓的家具也要擺好」)

逐間清點 1F(8 棟:4.5~8m 面寬 × 有無車庫)又挖出三個,每一個都是**規則沒涵蓋**:

| 洞 | 根因 | 解法 |
|---|---|---|
| 4.5/5.45/6m 面寬的**廚房沒有流理台** | 候選**只有「整面牆那麼長」一種**;走道型廚房(1.85×4.3m)唯一夠長的那面牆上就開著門 → 整條必撞門迴轉 → 一件都不擺 | `_counter_candidates` 每級 300mm **截短**,四面牆 × 兩個錨定端,**依長度排序**取放得下的最長那條 |
| 1.8m 寬的**餐廳沒有餐桌** | `table4` 是 `CENTER_ORIGIN`,擺位器只試房間正中央 9 個點 —— 而走道型餐廳的正中央就是通道 | 新家具 `table2`(2100×750,**靠牆**二人桌),走「沿四面牆找位置」那條路 |
| 全棟**沒有鞋櫃** | 這種房子沒有「玄關」這間房 → `FURNITURE_PROGRAM["foyer"]` 從來沒有觸發過 | `_entry_doors` **認門不認房名**:大門開在哪間房,鞋櫃就擺那間 |

⚠️ **截短不是我發明的**:`trim_counters_at_columns` 遇到柱早就這樣做了(師傅遇到柱是
切短、不是整排離牆)。這次只是把同一件事提前到「排候選」那一步。所以判準也一樣 ——
**放得下的最長那條**,不是「第一條合法的」(否則會為了湊長牆擺出 1.5m 的短檯面)。

⚠️ **`CENTER_ORIGIN` 的 9 個點是第三次咬人了**(車、四人桌、茶几都是)。這類家具的
位置本來就不是「挑個好位置」——不是所有東西都該交給擺位器。判斷方式:**問這件家具
的位置是被誰決定的**。被門決定 → 鞋櫃;被床決定 → 床頭櫃;被車位決定 → 車;被牆的
長度決定 → 流理台。這四件現在都是自己列候選。

⚠️ **順序又一次決定結果**:鞋櫃第一版擺在程式清單**後面**,落點離大門 **3.9m**
(客廳的另一頭)—— 因為沙發/電視櫃/單椅先把門邊佔滿了。改成跟洗澡設備一樣**先擺**
之後是 **1.3~2.0m**,而且沙發電視一件都沒少(它們讓得起,鞋櫃讓不起)。

⚠️ **鞋櫃排序用距離不用分數**:合法與否仍走既有碰撞引擎,只是把「挑分數最高」換成
「挑離門最近」(`_place_near_door`)。

**車庫版不放鞋櫃**是故意的:那扇是捲門,鞋櫃不會擺在車頭前面(`ENTRY_SKIP_KINDS`)。
**1F 浴廁沒有淋浴間也是對的** —— 它是樓梯旁的半套(馬桶+洗手台),台灣透天就是這樣。

**1F 成效**(8 棟):流理台 4→**8**、餐桌 6→**8**、鞋櫃 0→**5**(車庫版 3 棟不放);
唯一還是空的房間是**樓梯間**,那是動線不是居室。

#### ⚠️ 逐間清點要換一批尺寸再點一次(同一天,順手挖出來的)

上一輪報「床 28/28、更衣室 4/4」是真的,但那批案子**剛好沒中**。換成 seed=7 的
5.45/6/7/8m 一點:**3 間空更衣室、2 間沒有床**。兩個根因都是既有的:

1. `restore_essentials` 只救浴室與臥室,**沒救更衣室** —— 被動線修復器清空的
   更衣室沒有人補。(補的時候一樣要 `dodge_doors`,否則門弧會把候選全打回。)
2. **順序**:`drop_orphan_nightstands` 原本在 `restore_essentials` **後面**。
   床頭櫃是跟著床擺的,床被移走後那兩個小方塊還停在床頭原位,**正好卡住床要
   回去的地方** → 床永遠補不回來。兩支對調就好。

⚠️ 對調之後補回來的床是**新位置**,不會有床頭櫃(舊的已被當孤兒清掉)——
11 張補回來的單人床全部光禿禿。`refit_nightstands` 最後補一輪,但這件事踩了
**兩次**鐵則:

| 做法 | 結果 |
|---|---|
| 補完就收工 | 淺透天掃描 **2 案不合格**(`furniture_in_wall` / `circulation_blocked`)、車庫 `furniture_in_column` 3→**8** |
| 補完再過閃柱/動線/穿牆三道修復器 | 修復器反過來**把 2 張床搬走** |
| 補完過三道修復器,**有床不見就整批回退** | ✅ 床 36/36、更衣室 4/4、掃描與警告全部回到原基準 |

**加分項不得讓原本好好的東西壞掉** —— 這條在本檔已經是第四次登場
(`_fit_service` 壓浴廁、`_fit_margin` 留柱位、`_beds_ok` 退更衣室,現在加床頭櫃)。

### 折返梯中間那塊平台**在半層高**,不是地板(2026-08-27)

使用者指著 4.5m 面寬的 1F 說「這個樓梯做得不太對…沒有路可以到廚房」。量出來的
真相比「樓梯擺錯」深得多:

```
踏step  y 8050~10300   從地板爬到 +1.69m
折返平台 y 10300~11400  ← 整塊在 +1.69m(9 階 × 188)
廚房的門 y 11475       ← 正好開在折返平台這一段
```

門後面是一個 1.1m 深、頭頂 1.69m 的**凹洞**,人走不出去。

⚠️ **關卡為什麼沒擋**:`_stair_boxes` 的註解寫著「折返端平台是一塊站得住人的
平地,門開在正對平台的牆上是合理的」—— 那句話對**起步**平台是對的(它在樓層
地板高度),被原封不動套到**折返**平台上就錯了。又一次「規則在,但問錯問題」。
實測 60 案:**45 案**有這種門,其中 **16 案**前後段完全走不通(3.6~5.2m 面寬)。

#### 修法:使用者說「折返梯做比較窄也是可以的」——那才是實務做法

根因是幾何三方擠壓:`4.5m 室內 3950 = 浴廁 1500 + 折返梯 2300` → 只剩 150mm。
四件事一起改才收得乾淨:

| # | 改動 | 效果 |
|---|---|---|
| ① | `_stair_boxes`:**折返平台算障礙**(單跑直梯沒有這個問題,只有 UStair 要算) | 折返平台上的門 45→**0** |
| ② | `_fit_flight_width`:梯段寬**退讓階梯** 1925 → 法定下限 1600(住宅 75cm/段) | 走道從 150mm 變出來 |
| ③ | `_core_widths`:擠不出走道時浴廁退到 `BATH_TIGHT_W`(1200) | 4.4~4.9m 也有走道 |
| ④ | `_band_split_x(avoid=…)`:**後段**切點不得切爛走道口 | 動線不再繞道穿過廁所 |

⚠️ **折返平台被拉長成「剩下的全部」**是另一個獨立的 bug:`length=run_len` 把
樓梯間剩下的長度全給了平台,實測淺透天有一案平台深 **3.3m**(真正需要 0.75m)——
等於把 3.3m 的地板畫成半層高的平台。兩支 `_stair` 都加 `min(run_len, 梯跑+平台)`。

⚠️ **只有後段要讓開走道,前段不用**:前段面對的是樓梯的**起步平台**(那一端就是
樓層地板高度,整條牆都開得了門),後段面對的才是折返平台那一端。一開始兩段一起
套,7~8m 面寬的前段切不成兩間 → **8 條測試**冒出 35㎡ 的 `room_oversize`。

⚠️ **走道寬的時候不必讓**:判準是「走道要嘛整條給一間(另一間走姊妹房的內門,
例如 1F 的餐廳|廚房),要嘛寬到兩側各留得下一扇門(`PASSAGE_DOOR_NEED`)」。
一開始寫成「切點一律不得落在走道內」,8m 面寬(走道 3.4m)也被擋掉。

⚠️ **開天井時不做浴廁退讓**:天井就切在服務格裡浴廁沒用完的那一塊,服務格一窄
天井就小到 `code_check` 不認,等於白開。天井預設關,要開的人是拿走道換採光。

⚠️ **淺透天吃到同一條規則**:它的浴廁放在梯帶東端,而梯帶東端正是折返平台 ——
修好 ① 之後浴廁唯一開得了門的鄰居只剩南邊的廚房(4 件 `bath_door_to_kitchen`)。
梯帶只有 1.9m 深、擠不出旁邊的通道,所以**浴廁一律改放前段東側**(那個分支本來
就在,窄面寬一直走的就是它)。

⚠️ **窄透天沒接 `shift_openings_off_columns`**(本檔「規則存在,但關卡沒接」第
**六**次):排洞口時柱還不存在,後補的門(浴廁門/接通用門)因此可能落在柱上。
以前看不出來,幾何一動就冒出 3 件 `opening_on_column`(25~75mm)。

**成效**:折返平台上的門 45/60 → **0**;走不通 16/60 → **12**;窄透天掃描
**50/50**、淺透天 38/40、兩帶式 35/40、車庫 30/30(硬錯誤全 0,皆與基準相同)。

#### 3.6~4.3m 面寬:換**單跑直梯**(2026-08-27,使用者「好接著做」)

那個寬度連法定最窄的折返梯(2×750+100=**1600**)配上最窄的浴廁(1200)都擠不出
750mm 的走道 —— 幾何上兜不攏,不是擺法的問題:

```
3.6m 面寬 → 室內 3050 = 浴廁 1200 + 牆縫 150 + 折返梯 1600 + 導牆 150 → 走道 −50
                     = 浴廁 1200 + 牆縫 150 + 直梯   1100 + 導牆 150 → 走道 450…
```

單跑直梯只有**一個**梯段,寬度立刻省下 0.9m —— 那正是使用者參考平面圖的做法
(樓梯|天井+廁所|走道 三條並排)。`_straight_flight_w` / `_flight_plan` 負責選:
**走道優先**,折返梯留得下走道就用折返梯,擠不出來才換直梯。

⚠️ **代價在進深**:折返梯的梯跑只要一半,直梯要跑一整段。核心帶 4400 深
(可用 4250)配 17 級,踏面被壓到 **21~22cm**(法定下限 21cm,合法但比折返梯的
25cm 陡)。放不下就回 `None` 維持折返梯 —— **寧可沒有走道,也不要生不出來**。

⚠️ **`allow_straight` 預設關**:`_stair` 不是只有窄透天在用,**AI 產線
(`graph_layout`)也直接呼叫它**。那條產線的核是另一種幾何(中央核+管道間),
換梯型會讓門對不上、整層斷成兩塊(實測 `test_plan_check` 兩條倒:`floor_split`
+ `entry_door_narrow`)。窄透天自己傳 `allow_straight=True`。
這是「改共用函式要先問還有誰在用」的又一則。

**成效**:前後走不通 **16/60 → 0/60**(全面寬都通);折返平台上的門 45/60 → **0**;
窄透天掃描 **50/50**、淺透天 38/40、兩帶式 35/40、車庫 30/30(硬錯誤全 0)。

#### 走道要**貼著界牆**,而且要**開得出一扇門**(2026-08-27,使用者第二次指正)

使用者看完上面那批圖說:「我還是覺得有點奇怪,正常的走道應該靠在旁邊的牆上,
不會在樓梯跟廁所的中間。」對照參考平面圖確認是對的 —— 圖上是
**廁所 | 樓梯 | 走道** 三條並排,走道貼著共同壁。改法只有一行:
`_stair(hug="west")` 讓梯段改貼服務格那一側,走道自然落到另一側的界牆邊。

⚠️ **但「走得過去」不等於「用得了」。** 貼牆之後 40 案掃描冒出 **5 案**動線變成
「穿過廁所才到得了餐廚」(全在 4.4~4.9m)。根因是走道剛好只夠走(750~1000mm),
而後段那扇門要佔掉 `門寬 800 + 兩側牆角淨距 90×2 = 1030` —— 合法窗口只剩幾十
mm,補門機制挑不到位置,只好改走浴廁。所以走道的判準改成
**`PASSAGE_DOOR_NEED`(1030)+ `PASSAGE_DOOR_MARGIN`(250)**,達不到就換梯型。
連帶:**單跑直梯的適用上界從 4.3m 抬到 5.0m**(5.45m 是第一個「折返梯縮到 1800
之後旁邊還留得下一扇門」的面寬)。

⚠️ **一改幾何,綁在舊幾何上的式子就會靜靜地算錯。** 1F 後段的切點原本寫
`stair.origin[0] − 導牆/2`,那條式子的意思是「切在走道邊」—— 只有在**梯段貼東牆**
時才成立。梯段改貼西之後同一條式子跑到服務格東緣,整個樓梯間的寬度全歸東室:
7m 面寬的廚房 **17㎡**、8m **21㎡**(上限 11)。切點的依據要回到「兩間房各自
該多大」(`_band_split_x` 按面積上限比例分),走道那件事交給 `avoid` 管就好。
**判準要寫成它真正的意思,不要寫成當下剛好等價的座標。**

⚠️ **門躲柱時不吃窗的邊界淨距。** `shift_openings_off_columns` 算可用區間時一律
套 `WINDOW_EDGE_CLEAR`(300),門也一樣 —— 但門離牆兩端多遠是 `_door_pos_ok`
(門角淨距,分級退讓到 90)在管的。1.3m 的短牆因此憑空少掉 600mm、只剩 700,
850 的門怎麼算都塞不下,於是整支**靜靜地不動**,柱就繼續插在門框裡
(4.5m 面寬 2F 固定被吃 25mm)。**同一件事兩把尺**在本檔已經第五次。

⚠️ **`restore_essentials` 漏了馬桶。** 幾何一動,4.5m 的 2F 浴廁變成南北各一扇門
(南邊套內、北邊對走道),兩個門弧一夾,`_declutter_for_circulation` 就把**馬桶**
整個移掉 —— 而補回來的清單只有洗澡設備/床/更衣室。馬桶沒有「小一號」的替代品,
所以改**讓門去閃設備**(與更衣室同一招,`repair_doors` 後面會把小浴室的門收成
拉門)。「擺不下就略過是靜默的」這條坑,換一組尺寸就會再咬一次。

⚠️ **修門「補出來」的新門也要躲柱**(本檔「規則存在,但關卡沒接」第**七**次)。
`shift_openings_off_columns` 跑在第一次修門**之前**,而修門會補新的門(接通用門、
浴廁門)—— 那幾扇從此沒有人問過它壓不壓柱。車庫版 30 案有 **2 案**的門被角柱吃掉
45~112mm。修法是在最後一次修門**之前**再收一次(放後面就沒人管門扇會不會撞到)。

⚠️ **挪不動一扇門時,擋住它的常常不是柱,是旁邊那扇門。** 車庫北牆一次排三扇 850
的門(浴廁一扇、樓梯間兩扇),最西那扇被角柱吃掉 112mm,而它東邊只有 220mm 就是
下一扇 —— **逐扇試永遠找不到位置**。`_repack_doors_on_wall` 整排一起往旁邊推就排
得下(4m 的牆扣掉兩端柱還有 3.48m,三扇門連牆垛只要 2.95m)。重排只做「整道牆都是
門」的情況(窗牽動 §40 採光),而且要保住**左右順序、每扇門的鄰室、離牆角淨距**,
少一件就整批還原 —— 門一挪過界,那間房就變成要穿過別人家才進得去。

**這一輪的成效**:窄透天掃描 50/50、淺透天 **40/40**(原 38)、兩帶式 **36/40**
(原 35)、車庫 **30/30**(原 28),四條硬錯誤全 0。

### 參考平面「方案 B」的中段核:`core_style="ref"`(2026-08-28)

使用者拿了另一張 4.5×15m 的參考平面(無車庫版)說「這樣擺其實也不是不行,不過我
還是要做一版這種格局的」。對照下來差三件事,三件都做了:

```
        ┌──────────────┬────────┬──────┐  ← 北(接餐廚)
        │     天井      │  廁所  │  走  │
        ├──────────────┴────────┤  道  │
        │      樓梯(橫置)        │      │
        └───────────────────────┴──────┘  ← 南(接客廳)
```

| | 預設核 | 參考圖版 |
|---|---|---|
| 排法 | 浴廁 \| 樓梯 \| 走道 | 樓梯橫置 + (天井 \| 廁所) + 走道 |
| 廁所的門 | 開向餐廚(車庫版開向車庫) | **開在走道上** |
| 天井 | 沒有 | **有**(101 個樓層有 96 個排得下) |
| 樓梯吃掉的進深 | 3.65m(直梯順著進深跑) | **1.0~2.1m**(橫置) |

**橫置樓梯**:梯跑沿著**面寬**跑(`direction` 是 east/west),起步端在走道那一側。
面寬夠長就用單跑直梯(參考圖畫的就是這種),不夠就把折返梯**轉 90 度**(淺基地
產線用的同一招,梯跑只要一半)。實測 101 個樓層:直梯 42、轉 90 度的折返梯 59。

⚠️ **「貼著走道」不等於「門開在走道上」。** 把廁所搬到走道旁邊之後,實測 101 個
樓層仍有 **38 個**把門開向餐廚 —— 補門機制是「在牆上找第一個合法位置」,先挑到
哪一面就開哪一面,它不知道這張圖的重點是什麼。所以要 `_bath_door_to_hall` 明講。
這跟本檔「規則存在,但關卡沒接」是同一族:**幾何擺對了,不代表結果會對**。

⚠️ **走道是這個核唯一的動線**,而它只有一扇門的寬度 —— 前後段因此**切不成兩間**
(兩間房的門都要開在同一段 1.2m 的牆上)。代價是寬面寬的案子會多出 `room_oversize`
警告(40 案掃描 22 件)。這是這個排法本身的取捨,不是 bug;要切兩間就用預設核。

⚠️ **加分項不得讓原本生得出來的案子生不出來**(第五次登場):排不下時 `_core_ref`
回 `None`,靜靜退回預設核,不 raise。3.6m 面寬就是這樣。

**驗收**:40 案掃描(3.5~8m × 各自的進深區間)**硬錯誤 0、法規 0**,
廁所的門 **101/101** 開在走道上。用法:
`generate_narrow_building(..., core_style="ref")` / `HouseBrief.core_style` /
`preview_plan.py --ref-core`。

### AI 不再自己畫格局:選配版 `townhouse_options`(2026-08-28)

使用者指著網站下載的 DXF 說:

> 「我要讓 AI 做我這種方案 A、B,不要按照他的做法,他做的圖不是正確的,
>   不會分成這麼多的間格,AI 只要從我做的格局稍加修改變更就好。」

那張圖是 **AI 關係圖版**畫的,不是規則版 —— 認法很簡單:圖上有「管道間」
(只有 AI 版的核放管道間)、沒有「樓梯間/浴廁/餐廚」這幾個規則版固定的房名、
而且同一層出現**兩間**「客廳」。7m 面寬切成 8 格,那不是台灣街屋。

**問題不在搜尋,在前提**:讓 LLM 自由設計房間關係圖,等於要它憑空知道連棟街屋
長什麼樣。改法是換掉 LLM 的**職責**——骨架固定(`narrow_house` 的前後串聯 + 中段
核),LLM 只挑「一個設計師會挑的那幾個選項」:

```
core_style(方案 A/A'/B) · floors · bedrooms · garage · patio
mirror · open_kitchen · entry_frac
```

然後照樣跑「選 → 蓋 → 挑毛病 → 重選 → 留最高分」的收斂迴圈(fitness 與
`design_loop` 同一把尺)。變化仍然很多(3 款核 × 鏡射 × 開放餐廚 × 大門位置 ×
車庫 × 天井 × 樓層 × 房數),但**每一種都還是一張真實街屋的平面**。

實測同一個 7m 面寬,網站那條路徑:

```
舊(關係圖版) 客廳/客廳/餐廳/廚房/廁所/樓梯/管道間/陽台   ← 使用者看到的那張
新(選配版)   客廳/浴廁/樓梯間/餐廚
```

⚠️ **定義域要是兩條的聯集**。關係圖版的下限是 5m,而 **4~5m 正是最常見的街屋
面寬** —— `_ai_applicable` 只拿它去判的話,4.5m 在入口就被踢掉,永遠走不到選配版
(骨架明明收得下 3.5m 起)。`_ai_generate` 裡那個 raise 也是同一個坑。

⚠️ **LLM 的答案一定要夾**:它會給 10 層樓、3.5m 面寬配車庫、方案 A 配天井。
schema 擋得住型別,擋不住「物理上不可能」——`normalize_options` 拿骨架自己的常數
去夾(`min_depth_for` / `MIN_WIDTH`,不要在那邊另抄數字)。

⚠️ **「蓋不出來」不只是 raise**:有些組合**蓋得出來但圖不合格**(實測 3.6m 面寬配
方案 B 的某個變體,會生出「餐廚沒有門」)。`build_from_options` 的退讓階梯因此以
`plan_check` 過不過當判準,不是有沒有丟例外 —— 只看例外的話,退讓對這種案子完全
不會啟動。全部都不合格時回**第一個蓋得出來的**,交給呼叫端的關卡回報,不 raise
(那會讓使用者連一張可以看的圖都拿不到)。

⚠️ **隨機掃描沒抽到 ≠ 不存在**:三款核各 60 案隨機變體全過(60/60),但上面那個
3.6m 的壞組合就在那 180 案之外 —— 是**單元測試**先撞到的。兩種驗收是互補的。

**關係圖版沒有被刪掉**:面寬 >8m(骨架收不下)仍然走它。

### 第三款核:服務格搬到中間 `core_style="mid"`(2026-08-28)

使用者看完參考圖版之後說「還想做一款廁所門是開向走道的」—— 指的是**預設核**那一版
(樓梯順著進深跑,他先前說「這樣擺其實也不是不行」)。差別只有一件事:服務格從
最外側搬到**樓梯與走道中間**。

```
        ┌──────────────┬────────┬──────┐  ← 北(接後段)
        │              │  浴廁  │  走  │
        │    樓梯       ├────────┤  道  │
        │  (順著進深)   │        │      │
        └──────────────┴────────┴──────┘  ← 南(接前段)
```

三款核的分工(全部共存,由 `core_style` 選):

| | `default` | `mid` | `ref` |
|---|---|---|---|
| 排法 | 浴廁 \| 樓梯 \| 走道 | 樓梯 \| 浴廁 \| 走道 | 樓梯橫置 +(天井 \| 廁所)+ 走道 |
| 樓梯方向 | 順著進深 | 順著進深 | **橫置**(沿面寬) |
| 廁所的門 | 開向餐廚/車庫 | **開在走道上** | **開在走道上** |
| 天井 | 無 | 無 | **有** |
| 前後段切兩間 | 可以 | 寬面寬可以 | 不行(走道只有一扇門寬) |

⚠️ **浴廁一定要放北邊**,不是選擇:南邊那一段要留給樓梯的**起步平台** —— 人從前段
進來要先站得到平地才上得了樓,而起步端在南。浴廁放南邊的話,前段就只剩走道那
1.3m 進得來,樓梯反而接不上。

⚠️ **這一版買到的東西不只是門的方向**:樓梯旁邊不必再留走道(走道在浴廁的另一邊),
同樣的面寬梯段可以用得更寬 —— 同一批 101 個樓層,折返梯(踏面 25cm,好走)從預設核
的 **56 個**變成 **80 個**,直梯 45 → 21。窄面寬的透天「樓梯很陡」少掉一半。

⚠️ 三款的梯型分布(同一批尺寸,`staircmp` 量的):
`default` 直梯 45 / 折返梯 56、`mid` 21 / **80**、`ref` 42 / 59。
`ref` 的踏面中位數只有 **224mm**(其餘兩款 260)—— 橫置的梯跑受面寬限制,比較陡。

⚠️ **這個核不放天井**:三段已經把面寬用完。要天井請用 `ref`(把樓梯橫置才騰得出
第四段)。

⚠️ **量錯尺會得到沒有意義的數字**:第一版的「廁所的門通到哪」是用「離浴廁 50mm 內」
去抓的,結果把**後段那扇開在走道上的門**也算成浴廁的門(它就貼著浴廁的轉角)。
判準要改成「門洞落在浴廁**自己那幾道牆**上」。本檔「報表會說謊」系列的又一則。

**驗收**:40 案掃描 **硬錯誤 0、法規 0**,廁所的門 **101/101** 開在走道上。用法:
`core_style="mid"` / `preview_plan.py --mid-core`。

#### ⚠️ 順帶:整道牆重排要連**窗**一起(不然臨路南牆修不好)

`_repack_doors_on_wall` 原本限定「整道牆都是門」才做,理由是「窗牽動 §40 採光」。
那個顧慮是**錯的**:§40 算的是「那間房的開口總寬」,沿著**同一道牆**平移既不改
寬度也不換房間。而臨路的南牆正好是「窗 + 大門」的組合 —— 限定整道牆都是門,等於
這條規則對南牆永遠不生效(實測 4.1×11.5m 的大門被角柱吃掉 10mm,預設核也一樣中)。
改名 `_repack_openings_on_wall`,窗多兩條限制:離牆兩端 `WINDOW_EDGE_CLEAR`、
兩扇窗之間的牆垛 `WINDOW_PIER_MIN`(牽涉到門的用一般淨距)。

#### ⚠️ 還沒解:浴廁的門開向餐廚/車庫,不是開在走道上

參考平面圖是 **樓梯 | 天井 | 廁所 | 走道** —— 廁所貼著走道。現在的核是
**浴廁 | 樓梯 | 走道**,廁所被樓梯隔開,只好往南北兩邊的居室開門(1F 開向餐廚、
車庫版開向車庫)。`bath_door_to_kitchen` 沒抓到是因為那間的 kind 是 `dining`。
**這條只剩預設核還沒解** —— 參考圖版(`core_style="ref"`,見上一節)已經把廁所搬到
走道旁邊、門也開在走道上了。預設核要跟進的話,得把服務格搬到**樓梯與走道中間**:
總寬一樣(只是重新排序),但 `_core` / `_passage_span` / `_band_split_x` 的 `hall_x0`
/ `_master_suite` 的更衣室位置全都是照「服務格在最外側」寫的,是整個中段核的改法。

### 連棟街屋:基地→建築要用建蔽率,不是四面退縮(2026-08-25)

使用者把真實的透天基地丟進來(5×20m),程式算出「建築 1.0×16.0m」然後 raise。
根因:基地→建築一律用**四面各退 2m** —— 那是**獨棟**的規則。

台灣的透天厝是**連棟街屋**,左右與鄰戶共壁。使用者指著平面圖給了三個判斷依據
(圖上就看得出來,不必問屋主):

1. 左右兩道牆是**共同壁** —— 貫穿到底、整段沒有開口(獨棟/雙拼至少一側會畫窗)
2. 對外開口**只在前後兩端**(前面捲門/大門、後面後門),中間十幾米完全封閉
3. 中段開**天井** —— 側面沒窗,中間才需要另外找採光

推導因此變得很簡單(`design/zoning.py`):

```
建築面寬 = 基地面寬              ← 共壁,側邊不退縮
建築面積 ≤ 基地面積 × 建蔽率
⇒ 建築進深 = 基地進深 × 建蔽率    ← 面寬約掉了
剩下的進深 = 前院 + 後院(法定空地,不計建築面積)
```

**基地 5×20m、住宅區 60% → 建築 5×12m、前後院各 4m**,與使用者手算一致。

⚠️ **建蔽率一定要是參數。** 使用者拿舊市區街屋的平面圖去算,建蔽率約 **93%** ——
那在住宅區(60%)不可能過,只可能是商業區(80% 上下)或法規收緊前的既存老屋。
`COVERAGE_BY_ZONE` 是**常見值不是法源**,各縣市都市計畫書逐區規定,有實際數字就
直接給 `coverage`。

⚠️ **住宅區 60% 很緊**,連棟街屋要生得出來,基地至少要這麼深:

| 建築面寬 | 骨架最小進深 | 住宅區 60% 需基地深 | 商業區 80% 需基地深 |
|---|---|---|---|
| ≤6m | 9.5m | **15.8m** | 11.9m |
| 7~8m | 10.5m | **17.5m** | 13.1m |

所以「基地 4.5×15m」在住宅區只能蓋 9.0m 深 → 低於骨架下限 → raise。**這不是 bug,
是真實的限制**;同一塊地換成商業區就生得出來(12.0m)。

#### ⚠️ 共同壁的中心線**就是**地界線,不能再縮 `STRUCT_MARGIN`

第一版照舊縮了 275mm 留柱位,結果兩戶之間出現一條 27.5cm 的縫(現實中沒這種
東西),而且建蔽率莫名少用 7%(5m 寬的地只蓋 4.45m)。共同壁裡的柱是兩戶共用的,
本來就跨在界線上。改成側邊不縮(`mx = 0`)之後:

* 建蔽率剛好 60%/80%(不再是 53%)
* **室內看得到的柱角 0.307 → 0.068 ㎡** —— 柱整根埋進共同壁,反而更乾淨

前後仍要縮:那兩面是自己的外牆,柱得往院子側躲才不會凸進房間。

#### ⚠️ 兩個「尺寸基準」不能搞混

`dimension_basis` 決定使用者講的是基地還是建築。**只有 `"site"` 才套建蔽率** ——
講「建築物 5×15」的人已經自己扣好了,再套一次會變成「基地 5×15 → 建築 5×9」直接
raise。`scripts/scan_plans.py` 與 `scripts/preview_plan.py` 的尺寸一律是**建築物**
尺寸,兩支都要明寫 `dimension_basis="building"`(踩過:不寫就整批掃不出來)。
`preview_plan.py --site --zone 商業區` 才是用基地基準預覽。

#### 圖上要看得到建蔽率

只給建築尺寸的話,看圖的人會以為基地尺寸被無視;而「這塊地為什麼只蓋這麼深」的
答案就是建蔽率。`FloorPlanSpec.lot_note` 在地界線下方寫一段:

```
基地 5.00×20.00m = 100.0㎡    建築 5.00×12.00m = 60.0㎡
建蔽率 60%(住宅區上限 60%)    前院 4.00m / 後院 4.00m(法定空地)
```

⚠️ 連棟街屋 `spec.setback = 0`(共同壁中心線=地界線),建築線會與地界線重合 ——
那是**對的**,街屋的前後院是建蔽率吃剩的法定空地,不是建築線退縮。所以
`apartment_plan` 在 `setback == 0` 時不再另外畫一條重疊的建築線。

### 真實透天的尺寸:面寬 4~8m、進深 12~18m(使用者 2026-08-25 給的實務值)

使用者給了台灣一般單棟透天厝的實際尺寸,**而且明講是「建築物」尺寸不是基地**:
面寬 4~5m 最常見、較大的到 6~8m;進深 12~18m,**15m 左右是主流**。
對照當時的能力,缺口有兩個(層高那項另計,尚未做):

| | 改前 | 改後 |
|---|---|---|
| 面寬 | 3.5~7m(8m 直接 raise) | 3.5~**8m** |
| 進深 | <4m 給 12.0m、其餘一律 13.5m | **實測表** 12.5~16.5m 依面寬 |

#### 面寬放寬到 8m:難的不是「排不排得下」,是房間大小

把上限改成 8m 之後 8m 就生得出來了,硬錯誤與法規**一件都沒有** —— 因為窄透天的
骨架是「前段|核|後段」串聯,面寬變寬只是每一段變寬。問題在**一段就是一整間房**:

```
面寬 7.0m(改前的合法上限)→ 臥室 27.9㎡   ← 已經每個 seed 冒 5 個 room_oversize
面寬 8.0m                  → 臥室 32.2㎡
```

所以配套是三件事,缺一不可:

1. **`_band_split_x`:前/後段左右切成兩間。** 切點**不能自由選** —— 這一段只有一面
   朝向中段核,兩間房的門都得開在那面牆上,而那面牆不是整條都貼著樓梯間:貼著
   **浴廁**的那一段開不了門。切太靠西 → 西半間變成「要穿越隔壁臥室才進得去」。
   ⚠️ 浴廁在南(預設)擋的是**前段**、在北擋的是**後段**,兩段不能共用同一個值。
2. **切點按兩間房各自的合理上限按比例分**,不是一律切中間:1F 後段是廚房|餐廳,
   廚房上限 11㎡ < 餐廳 14㎡,對半切會讓廚房超標。同 kind 時退化成切中間。
3. **`_core_widths` 夾住服務格寬度。** 服務格本來拿「面寬減掉樓梯間」的全部剩餘,
   8m 面寬就變成 4.4m 寬 —— 浴廁用不到,多的會被 `_core` 當成空格丟給隔壁居室,
   **反而把那間房撐得更大**。多的寬度給樓梯間(=旁邊通道變寬),前後段才切得動。

**成效**(3 seeds × 7 面寬 × 4 進深):生不出來 9→**0**、房間過大 28→**6**、
硬錯誤與法規維持 **0**。剩下的 6 個是 8×18m 的 1F 後段(44㎡ 要切**三間**才夠),
屬於「改設計才救得動」= warning 的定義。

#### ⚠️ 順帶挖出一個通用 bug:補門時「先挑位置、再看鄰室是誰」

`_add_interior_door` 的流程是「在這道牆上找第一個合法位置 → 算門的另一邊是哪間 →
不是指定的鄰室就**整道牆放棄**」。但一道牆可能同時貼著**兩間**鄰室(前段北牆:
西半浴廁、東半樓梯間),而候選位置是從中點開始試的 —— 中點正好落在浴廁那半。

於是「這間一定要有一扇門直通公共動線」這條修復**對這種牆從來沒有生效過**,
8m 面寬每個 seed 都出 `through_bedroom`。改成**換位置再試**(鄰室檢查移進候選迴圈)。
這跟 `column_centers is None`、`單跑直梯` 是同一種盲點:規則在,但走不到。

#### 進深:上限是量出來的表,加一道退讓階梯兜底

舊值只有兩檔(12.0 / 13.5m),比實際能力保守 1~2.5m —— 5m 面寬其實撐得住 14.45m、
8m 撐得住 16.45m。量法:每個面寬從最小進深起、每次 +0.5m,3 seeds × 3 層全部要過
plan_check + code_check,**再深一級就出 `daylight_area`** 的那一級就是上限。
擋住的**一律**是 §40 採光(透天只有前後兩面能開窗),沒有別的規則。

```
_MAX_DEPTH_POINTS = 建築面寬 3500→12500 … 5450→15450 … 7450→16450(mm)
```

⚠️ 表是**起點估計**:只量過那幾個面寬,中間靠內插。真正的答案由 `_fit_depth`
當場拿 code_check 量,量不過就收 500mm 重蓋(「實際量過才算數」)。所以表可以
樂觀一點,不必抓最保守。⚠️ 退讓必須在**整棟**這層做 —— 各層各退各的,上下樓外牆
就對不齊了(跟 `_fit_margin` 不讓各層各留各的柱位是同一個道理)。

⚠️ 6.45m 那個**凹陷**(14950,比兩邊都低)是量出來的、不是筆誤:前後段在那個尺寸
開始切成左右兩間,每間能開窗的外牆長度砍半,窗反而更難開滿。

#### ⚠️ 加分項不得讓原本生得出來的案子壞掉:`min_service` 的退回

6m 級面寬的服務格只有 1.875m(還沒碰到 `BATH_MAX_W`,上面第 3 點夾不到它),
但樓梯間也因此不夠往西、前段切不成兩間 → 28㎡ 的臥室。解法是把浴廁再壓到最小寬
(1.5m)換一間大小正常的房間。**但壓窄之後梯段/導牆/通道的相對位置全變了**,
掃描實測 6.8×17.2m 會讓樓梯間動線走不通。所以 `_fit_service` 包一層:壓了如果
出硬錯誤就不壓(與 `_fit_margin` 留柱位同一條鐵則)。

#### ⚠️ 又一次「同一件事、兩個地方兩把尺」:陽台 vs §40

進深放寬之後 7×15m 的陽台整個消失。原因不在深度,在 `balcony.py` 判「放了落地門
還開不開得出 §40 的窗」時**沒把落地門自己算進採光開口** —— 而 `code_check` 一直
是算的(「§40 講的是採光用窗**或開口**」)。等於用比關卡更嚴的尺擋掉自己的陽台,
實測差 **30mm**。修法是扣掉門寬再比。這個 bug 類型在本檔已經出現第三次了。

### 「房間過大」與走道:量對尺、房間數要跟著面積長（2026-08-13）

**先確認你量的是對的東西。** 19×13m 三層原本 10 個 `room_oversize`，其中 **3 個是尺錯了**：

| 房間 | 拿什麼量它 | 為什麼錯 |
|---|---|---|
| 餐廚 34㎡ | 「餐廳」的上限 | 餐廚 = 餐廳+廚房併成一間，上限要**相加** |
| 主臥 27㎡ | 「次臥」的上限 | 規則版兩帶式的主臥 `kind` 也是 `bedroom`，只有名字帶「主臥」 |

判準抽成 `plan_check.oversize_band(room, table)`。⚠️ `benchmark.check_rooms` 有一份同樣
意圖的舊實作（`_req_for` / `_MERGED_HINTS`），**改判準時兩邊要一起改**。

**剩下的才是真的**：`_west_zone_cut` 原意是「不讓一間無限長大」，但它**只切一刀** →
變成「讓兩間一起無限長大」。改成 `_west_zone_cuts` 切到每間都在合理範圍
（19×13 的 1F 北帶 32.9㎡ → 書房 16.5 + 家庭廳 16.5）。用途一律由
`select_overflow_program` 決定（跟南帶同一支），所以切三間是「書房→家庭廳→多功能室」。

⚠️ 切點要跑**退讓階梯**，而且驗的不只是寬度：
①每格北牆塞得下窗（900）②帶分界牆塞得下門（750）③**最後一格是餐廳/餐廚**，
它對客廳開的是**寬通道**（獨立廚房 1.2m、開放餐廚 1.8m，開在 `[cut, xb]` 上）。
少驗第 ③ 項：切完剩 1.186m，門過得了、通道差 **14mm** 過不了 → 整份設計 raise。

**走道改成功能性判準**（使用者 2026-08-12：「走道是真的很大的房子才會需要走道」）。
`has_hall = n >= 3` 是錯的——走道在這個骨架裡的用途**只有一個**：接東側臥室
（它有一段坐在服務核上方，南邊不是客廳而是廚房）。改成
`has_hall = n >= 2 and pos_direct is None`。120 組隨機尺寸：有走道 51 → **30 案**，
生得出來的案子 86 → **86**（一個都沒少）。省下的面積併進客餐廳（46.2 → 61.6㎡）。

### ❌ 走錯過的路：為了縮小房間把牆搬離柱軸線（2026-08-13，已退回）

「餐廚 58㎡」（15×12m）的成因是**廚房整整吃掉一個柱跨**（5.8m × 6m = 35㎡）。
試過把 `xk`（廚｜餐分界牆）從軸線挪開、照廚房面積上限給寬——**方向就是錯的**：

> 牆一離開軸線，那根柱就只剩 900mm 的柱包短牆遮著＝**為了湊面積讓柱子凸進房間**。

`room_program` 模組頭早就寫了「面積是目標不是命令，柱網/結構/牆線優先」，
`test_house_kitchen_wall_sits_on_axis`（12 個參數）釘的就是這條。實測 15 條測試全紅、
隨機掃描還冒出硬錯誤。**要解得從柱跨本身下手**（讓那個尺寸多切一跨），不是搬牆。

### 樓梯規則看不見單跑直梯（2026-08-13，使用者指出「樓梯跟門都會重疊」）

跟 `column_centers is None` 是**同一種**盲點：

```python
run = min(st.flight_run, st.length)   # flight_run 只有折返梯 UStair 有
except Exception:
    continue                          # ← 單跑直梯 Stair 整座被當成不存在
```

兩帶式與集合住宅用的是**單跑直梯**（`Stair`，沒有 `flight_run`）。於是
`stair_blocks_door`（門不得直接開在階梯上）與 `stair_side_open`（梯段不得一側沒牆）
**對那兩條產線從來沒有生效過** —— 實測**每一層**的樓梯間門離第一階只有 150mm
（一個牆縫），一開門就踩上踏step。直梯要改讀 `flight_length`。

修產生器時的陷阱：`_house_stair` 以前只在**頂端**留平台，底端從牆縫直接開始。
補 `HOUSE_STAIR_ENTRY_LANDING = 600` 之後梯跑變短，**級數不能再用「填滿可用長」回推**
（級數會變少 → 每階升高破 `MAX_RISER`）。級數改由**層高**決定
（`ceil(FLOOR_HEIGHT / MAX_RISER)`），踏面在 `[MIN_TREAD, HOUSE_STAIR_TREAD]` 之間縮。
`STAIRWELL_MIN_DEPTH` 4700 → **4900**，另加退讓階梯（塞不下就一級一級讓平台）。

### 留柱位漏掉了地下車庫（2026-08-13）

`STRUCT_MARGIN` 把建築縮小後，20×20m 基地的南帶掉到 3.9m，而 **B1F 車庫就在南帶
正下方** → 「車庫進深太淺」整份設計失敗（benchmark B10）。當時只驗了地上層。

鐵則是「留柱位不得讓原本生得出來的案子生不出來」，所以有車位時南帶讓深到
`GARAGE_MIN_DEPTH`（4800），**餘量只從院子拿**（院子少 0.9m 換車停得進去，不用選），
上限仍守採光深度。⚠️ 不能吃臥室帶，那會讓臥室變窄（已有測試釘住）。

⚠️ **測試不要釘 `基地 − 退縮` 當建築外框**：`STRUCT_MARGIN` 讓建築往內縮之後，
寫死的框比真正的建築大一圈 → 大門與窗被判成「不在外牆上」，冒出假的 `no_entry`
（實測 24 個變體全滅）。外框一律用 `plan_check.building_env(spec)` 由 spec 自己推。
同理，測柱網對齊要移**柱位本身**，不是 `grid_origin`——柱位定案後會寫成具體清單，
`grid_origin` 只剩畫軸線用。

---

## 目前能力邊界

**做得到：** 窄透天（建築寬 5~30m、深 ≥9m）多層、含柱/樓梯/管道間/中庭/家具，
每層動線通、垂直核對齊。實測 15×15=91 分、20×20=92、24×16=92、34×22=90，0 硬錯誤。

**離「合格圖面」還差多少**：`python -m src.design.gap_analysis` 會拿產線畫出來的圖
去對照丙級檢定術科的參考平面圖（台灣的圖；**不用簡體字的圖當基準**），逐項量測後
更新 [docs/gap_analysis.md](docs/gap_analysis.md)。目前 35 項元素涵蓋率 64%。

**圖面標註**（2026-08-03 補，`FloorPlanSpec` 預設開啟，不像 `schedules` 是可選旁註）：
門窗編號 `opening_marks`（與門窗表同一來源 `schedule.opening_codes`）、牆厚引線
`wall_notes`（「15cm RC Wall」）、剖切符號 `section_mark`（A—A）；樓梯改標
`UP 16`／`DN`（`stair.flight_label`，級數由樓梯自己算）。

**做不到 / 尚未做：**
- 寬度 <3.5m 或深度 <9.5m（物理下限，骨架排不下）
- **寬度 8~10m 沒有任何產線收**（窄透天上限 8m、兩帶式下限 10m）
- 寬度 >30m
- **一樓挑高**：全棟共用一個 `floor_height`（3.2m），店面/挑高 3.6~4.2m 做不到
- **1F 車庫只接窄透天**（`generate_narrow_building(garage=True)` / `HouseBrief.
  car_spaces > 0` / `preview_plan.py --garage`：前段整段停車 + 臨路捲門，客廳上 2F）。
  一台車位；**要建築進深 ≥13.1m**，3.5m 面寬放不下。兩帶式的 `car_spaces` 仍是
  **地下車庫**，兩者不是同一件事。機車位/車道坡度/車庫地坪洩水都還沒做
- 集合住宅不走 AI 產線（只有規則版）
- **天花板尚未實作** — 這是「對照真實審定圖」還缺的一塊
- **陽台只接規則版透天**（`layout/balcony.py`：二樓以上前後挑出 1.2~2.0m，落地
  橫拉門，不算居室）；AI 關係圖版與兩帶式尚未配陽台。陽台面積也還沒進面積計算表
  （真實圖會列成附屬建物），§41「開口外側有陽台」的採光折減未實作
- 樓梯/電梯井在剖面圖尚未剖出

---

## 工作方式

1. 改動前先跑 `python -m pytest -q` 確認基準是全綠的。
2. 動格局引擎的話，**跑隨機掃描驗證**（過去用 108~208 案掃描抓出系統性錯誤），
   只跑單一案例會漏掉。目標是硬錯誤 0。
3. 每個改動都要附測試。測試不能為了通過而放寬既有斷言。
4. 使用者驗收的方式是「看渲染出來的圖」，所以做完請產出預覽讓他看。
5. commit message 用中文，格式 `type(scope): 說明`，例如
   `fix(layout): 梯段兩側一定有牆 — 補導牆 + 第 9 條硬規則 stair_side_open`。

---

## 延伸文件

| 文件 | 內容 |
|---|---|
| [README.md](README.md) | 快速開始、專案結構、分析工具用法 |
| [docs/LAYOUT_ENGINE.md](docs/LAYOUT_ENGINE.md) | 格局引擎：分析堆疊、各層 API、評分公式 |
| [docs/ARCHITECTURE_V0.7.md](docs/ARCHITECTURE_V0.7.md) | 架構快照：分層、誰可以寫 spec、已知限制 |
| [docs/DEVELOPMENT_GUIDE.md](docs/DEVELOPMENT_GUIDE.md) | 開發原則（九步流程、工程紀律） |
| [ROADMAP.md](ROADMAP.md) | 階段 A~E 的原始路線圖（**只到 E1，之後的進度看 CHANGELOG**） |
| [CHANGELOG.md](CHANGELOG.md) | 版本變更紀錄 |
