# 核心:產生/讀取 DXF
ezdxf==1.4.4

# 核心:讀取繪圖標準設定檔(config/standards/*.yaml)
PyYAML>=6.0

# 核心:2D 幾何運算(牆角接合的多邊形聯集;日後房間布林/剖面線邊界也會用)
shapely>=2.0

# 選用:把 DXF 渲染成 PNG 預覽(沒有 AutoCAD 時檢查用)
matplotlib>=3.7

# C2:自然語言介面(中文需求 → 設計需求),呼叫 Gemini API 解析
google-genai>=1.0

# E1:網頁介面(FastAPI 後端;uvicorn 是啟動用的伺服器)
fastapi>=0.110
uvicorn[standard]>=0.29

# 開發:單元測試(httpx 給 FastAPI 的 TestClient 用)
pytest>=7.0
httpx>=0.27
